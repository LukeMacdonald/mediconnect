import 'package:flutter/material.dart';

class ViewHealthStatus extends StatelessWidget {
  final int id;
  const ViewHealthStatus({Key? key,required this.id}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
